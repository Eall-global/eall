import FooterColumn from "./FooterColumn";
import { solutionLinks } from "./footerData";

const FooterSolutions = () => {
  return <FooterColumn title="Solutions" links={solutionLinks} />;
};

export default FooterSolutions;
