import FooterColumn from "./FooterColumn";
import { productLinks } from "./footerData";

const FooterProducts = () => {
  return <FooterColumn title="Products" links={productLinks} />;
};

export default FooterProducts;
